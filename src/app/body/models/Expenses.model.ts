export class Expenses {
    constructor(public fees: number, public food: number, public chemistry: number, public clothes: number,
        public treatment: number, public transport: number, public house: number, public another: number, ) {
    }
};